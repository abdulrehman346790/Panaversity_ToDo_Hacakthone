name: frontend-service-skill
purpose: A reusable Claude Code Agent Skill that standardizes how the Next.js frontend communicates with the FastAPI backend, ensuring type safety and cleaner UI components.
instructions: Always separate API calls from UI components. Use the API_PATTERN.ts template for creating the API client.